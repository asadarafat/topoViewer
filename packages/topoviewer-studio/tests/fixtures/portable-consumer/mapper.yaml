version: 1
identity:
  sourceId: studio-portable-consumer
  sourceIdLabel: source_id
rules:
  - id: link-health
    metric: topoviewer_link_up
    select: link
    join: link_id
    value: up
    states:
      down: "==0"
    style:
      down:
        lineColor: "#d32f2f"

