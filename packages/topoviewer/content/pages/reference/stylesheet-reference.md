<!-- Generated by scripts/sync-object-reference.mjs. Do not edit directly. -->
# Stylesheet Reference

This page is generated from `packages/topoviewer/src/core/styleDefaults.ts`. It is the canonical public table for stylesheet keys, data types, accepted values, defaults, and intended use.

Use [Style Your First Topology](../start/style-your-first-topology.md) and [TopoViewer Stylesheet](topoviewer-stylesheet.md) for workflow and recipes. Use this page when you need the exact key contract.

## Node Style Keys

| Key | Data Type | Values | Default | Use |
|---|---|---|---|---|
| `backgroundColor` | color | Any CSS color or supported theme variable. | Falls back to the selected icon fill. Fallback: `#6ea8fe`. | Node body fill. |
| `badgeBackgroundColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Compact node badge background color. |
| `badgeBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Compact node badge border color. |
| `badgeBorderWidth` | integer | Finite integer number. | Falls back to the compact badge CSS border width. Fallback: `1`. | Compact node badge border width. |
| `badgeColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Compact node badge text color. |
| `badgeFontSize` | integer | Finite integer number. | Falls back to the compact badge CSS font size. Fallback: `9`. | Compact node badge font size. |
| `badgeFontWeight` | text | String value. | Falls back to the compact badge CSS font weight. Fallback: `850`. | Compact node badge font weight. |
| `badgeLabel` | text | String value. | Aggregate nodes can derive badge text from hidden member count. | Compact node badge label. |
| `badgeMinHeight` | integer | Finite integer number. | Falls back to the compact badge CSS minimum height. Fallback: `16`. | Compact node badge minimum height. |
| `badgeMinWidth` | integer | Finite integer number. | Falls back to the compact badge CSS minimum width. Fallback: `16`. | Compact node badge minimum width. |
| `badgeOffset` | integer | Finite integer number. | Falls back to the compact badge CSS corner offset. Fallback: `7`. | Corner offset used for positioned node badges. |
| `badgePadding` | integer | Finite integer number. | Falls back to the compact badge CSS padding. Fallback: `2`. | Compact node badge padding. |
| `badgePosition` | enum | `topLeft`, `topRight`, `bottomLeft`, `bottomRight` | Defaults to `topRight`. | Compact node badge placement. |
| `borderColor` | color | Any CSS color or supported theme variable. | Falls back to the selected icon stroke. Fallback: `#d8e8ff`. | Node body border color. |
| `borderDashPattern` | numberList | Space-separated string, comma-separated string, number, or number array. | No TopoViewer default; authored only. | Explicit SVG dash pattern for the node body border. |
| `borderOpacity` | number | Finite number. | No TopoViewer default; authored only. | Node body border opacity. |
| `borderStyle` | enum | `solid`, `dashed`, `dotted` | Defaults to `solid`. | Node body border pattern. |
| `borderWidth` | integer | Finite integer number. | Defaults to `4`. | Node body border width. |
| `display` | enum | `element`, `none` | Defaults to `element`. | Set none to hide the node. |
| `draggable` | boolean | `true`, `false` | Defaults to `true`. | Whether the node can be dragged. |
| `height` | integer | Finite integer number. | Defaults to `60`. | Visible node body height. |
| `icon` | text | String value. | Falls back to router.generic when no icon is selected. Fallback: `router.generic`. | Icon key selected from icons. |
| `iconBackgroundColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Background behind icon content inside the node body. |
| `iconColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Text glyph color. |
| `iconFit` | enum | `contain`, `cover`, `fill` | Falls back to the stylesheet image default. Fallback: `contain`. | Object-fit behavior for SVG/image icons. |
| `iconHeight` | integer | Finite integer number. | No TopoViewer default; authored only. | Accepted for compatibility; the current renderer fits icons to the node body. Prefer iconPadding. |
| `iconOpacity` | number | Finite number. | No TopoViewer default; authored only. | Icon glyph/image opacity. |
| `iconPadding` | integer | Finite integer number. | No TopoViewer default; authored only. | Insets icon content inside the node body. |
| `iconSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Accepted for compatibility; the current renderer fits icons to the node body. Prefer iconPadding. |
| `iconWidth` | integer | Finite integer number. | No TopoViewer default; authored only. | Accepted for compatibility; the current renderer fits icons to the node body. Prefer iconPadding. |
| `labelBackgroundColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Node label background color. |
| `labelBackgroundOpacity` | number | Finite number. | No TopoViewer default; authored only. | Node label background opacity. |
| `labelBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Node label border color. |
| `labelBorderWidth` | integer | Finite integer number. | Falls back to the node label CSS border width. Fallback: `0`. | Node label border width. |
| `labelCollisionPolicy` | enum | `none`, `avoid`, `fade`, `hide` | Defaults to `avoid`. | How node labels and metadata behave when automatic placement cannot avoid overlap. |
| `labelColor` | color | Any CSS color or supported theme variable. | Falls back to --topoviewer-fg-strong. | Node label color. |
| `labelFontSize` | integer | Finite integer number. | Falls back to the node label CSS font size. Fallback: `10`. | Node label font size. |
| `labelFontWeight` | text | String value. | Falls back to the node label CSS font weight. Fallback: `620`. | Node label font weight. |
| `labelOpacity` | number | Finite number. | No TopoViewer default; authored only. | Node label opacity. |
| `labelPadding` | integer | Finite integer number. | No TopoViewer default; authored only. | Node label padding. |
| `labelPosition` | enum | `top`, `right`, `bottom`, `left`, `center` | Defaults to `bottom`. | Node label placement. |
| `labelTextAlign` | enum | `left`, `center`, `right` | Falls back to centered node label text. Fallback: `center`. | Node label text alignment. |
| `labelTextMaxWidth` | integer | Finite integer number. | No TopoViewer default; authored only. | Node label maximum width. |
| `labelTextOverflow` | enum | `clip`, `ellipsis` | No TopoViewer default; authored only. | Node label overflow behavior. |
| `labelTextWrap` | enum | `none`, `wrap` | No TopoViewer default; authored only. | Node label wrapping behavior. |
| `labelXOffset` | integer | Finite integer number. | Defaults to `0`. | Pixel X offset after node label placement. |
| `labelYOffset` | integer | Finite integer number. | Defaults to `0`. | Pixel Y offset after node label placement. |
| `labelZIndex` | integer | Finite integer number. | No TopoViewer default; authored only. | Independent draw order for the node label. |
| `metaColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Node metadata color. |
| `metaFontSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Node metadata font size. |
| `metaFontWeight` | text | String value. | No TopoViewer default; authored only. | Node metadata font weight. |
| `metaZIndex` | integer | Finite integer number. | No TopoViewer default; authored only. | Independent draw order for node metadata when rendered as a collision-managed overlay. |
| `minZoomedLabelFontSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Hide label below this effective zoomed font size. |
| `nodeLayout` | object | Any valid value for the documented data type. | No TopoViewer default; authored only. | `Nested node content layout. Use { type: card } with explicit shape: roundRectangle. Supported card values: type ${nodeLayoutTypes.join(', ')}, direction ${nodeLayoutDirections.join(', ')}, icon placement ${nodeLayoutIconPlacements.join(', ')}, content align ${nodeLayoutContentAlignments.join(', ')}.` |
| `opacity` | number | Finite number. | No TopoViewer default; authored only. | Node opacity. |
| `outlineColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Visual outline color around the node body. |
| `outlineOpacity` | number | Finite number. | No TopoViewer default; authored only. | Visual outline opacity around the node body. |
| `outlineWidth` | integer | Finite integer number. | No TopoViewer default; authored only. | Visual outline width around the node body. |
| `selectable` | boolean | `true`, `false` | Defaults to `true`. | Whether the node can be selected. |
| `shape` | enum | `rectangle`, `square`, `circle`, `ellipse`, `triangle`, `roundRectangle`, `bottomRoundRectangle`, `cutRectangle`, `barrel`, `rhomboid`, `diamond`, `pentagon`, `hexagon`, `concaveHexagon`, `heptagon`, `octagon`, `star`, `tag`, `vee`, `polygon` | Defaults to `rectangle`. | Node body shape. |
| `shapePolygonPoints` | text | String value. | No TopoViewer default; authored only. | Custom polygon points when shape is polygon. |
| `statusColor` | color | Any CSS color or supported theme variable. | Aggregate nodes can derive status color from worst known severity. | Compact status marker color. |
| `statusPlacement` | enum | `topLeft`, `topRight`, `bottomLeft`, `bottomRight`, `center` | Defaults to `bottomRight`. | Compact status marker placement. |
| `statusSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Compact status marker size. |
| `underlayColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Visual underlay fill behind the node body. |
| `underlayOpacity` | number | Finite number. | No TopoViewer default; authored only. | Visual underlay opacity. |
| `underlayPadding` | integer | Finite integer number. | No TopoViewer default; authored only. | Visual underlay padding around the node body. |
| `width` | integer | Finite integer number. | Defaults to `82`. | Visible node body width. |
| `zIndex` | integer | Finite integer number. | Defaults to `10`. | Draw order for the node body. |

## Link Style Keys

| Key | Data Type | Values | Default | Use |
|---|---|---|---|---|
| `directionalStrokes` | boolean | `true`, `false` | Defaults to `false`. | Render declared link directions as opposing strokes on one physical link corridor. |
| `directionCenterGap` | integer | Finite integer number. | Defaults to `48`. | Gap between opposing directional arrowheads near the center of the link. |
| `directionLabelOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel offset applied to directional labels. |
| `directionLabelPlacement` | enum | `center`, `source`, `target`, `outside` | Defaults to `center`. | Directional label placement on the link corridor. |
| `directionLabelRotation` | enum | `none`, `auto` | Defaults to `none`. | Directional label rotation behavior. `auto` aligns labels to the rendered directional segment while keeping text upright. |
| `directionOverlayLayer` | text | String value. | No TopoViewer default; authored only. | Toggle ID that controls directional strokes and labels without hiding the base link object. |
| `directionStartGap` | integer | Finite integer number. | Defaults to `14`. | Inset between node boundary and each visible directional stroke. |
| `endpointLabelAutoPosition` | boolean | `true`, `false` | Defaults to `true`. | Shared default for source and target endpoint label auto placement. |
| `endpointLabelDistance` | integer | Finite integer number. | Defaults to lineWidth plus 14, with minimum 18. | Shared preferred endpoint label distance. |
| `endpointLabelMaxDistance` | integer | Finite integer number. | Defaults to endpointLabelDistance plus room for collision resolution. | Shared maximum endpoint label distance during auto placement. |
| `endpointLabelOverlayLayer` | text | String value. | No TopoViewer default; authored only. | Toggle ID that controls both source and target endpoint labels without hiding the edge object. |
| `endpointLabelSideOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Shared perpendicular offset used during endpoint label auto placement. This is applied before the final sourceLabelXOffset/YOffset and targetLabelXOffset/YOffset nudges. |
| `interactionWidth` | integer | Finite integer number. | Defaults to max(12, lineWidth + 10). | Pointer hit area. |
| `labelBorderColor` | color | Any CSS color or supported theme variable. | Falls back to --topoviewer-edge-label-border. | Shared edge label border color. |
| `labelBorderWidth` | integer | Finite integer number. | Falls back to edge label CSS border width. Fallback: `1`. | Shared edge label border width. |
| `labelInteractive` | boolean | `true`, `false` | Defaults to `true`. | Whether edge labels receive pointer events. |
| `lineFill` | enum | `solid`, `linearGradient` | Defaults to `solid`. | Line fill model. |
| `lineGradientStopColors` | text | String value. | No TopoViewer default; authored only. | Gradient stop colors when lineFill is linearGradient. |
| `lineGradientStopPositions` | text | String value. | No TopoViewer default; authored only. | Gradient stop positions when lineFill is linearGradient. |
| `lineOutlineColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Outline color behind the edge line. |
| `lineOutlineWidth` | integer | Finite integer number. | No TopoViewer default; authored only. | Outline width behind the edge line. |
| `sourceDistanceFromNode` | integer | Finite integer number. | No TopoViewer default; authored only. | Move rendered source endpoint inward from node boundary. |
| `sourceLabelAutoPosition` | boolean | `true`, `false` | Falls back to endpointLabelAutoPosition, then true. | Whether the source endpoint label is automatically positioned near its endpoint. |
| `sourceLabelBackgroundColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Source endpoint label background. |
| `sourceLabelBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Source endpoint label border color. |
| `sourceLabelBorderWidth` | integer | Finite integer number. | No TopoViewer default; authored only. | Source endpoint label border width. |
| `sourceLabelColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Source endpoint label color. |
| `sourceLabelDistance` | integer | Finite integer number. | Falls back to endpointLabelDistance. | Preferred distance between the source endpoint and its label. |
| `sourceLabelFontSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Source endpoint label font size. |
| `sourceLabelFontStyle` | text | String value. | No TopoViewer default; authored only. | Source endpoint label font style. |
| `sourceLabelFontWeight` | text | String value. | No TopoViewer default; authored only. | Source endpoint label font weight. |
| `sourceLabelMaxDistance` | integer | Finite integer number. | Falls back to endpointLabelMaxDistance. | Maximum distance the source endpoint label may move from its endpoint during auto placement. |
| `sourceLabelOpacity` | number | Finite number. | Defaults to `1`. | Source endpoint label opacity. |
| `sourceLabelOverlayLayer` | text | String value. | Falls back to endpointLabelOverlayLayer. | Toggle ID that controls the source endpoint label. |
| `sourceLabelSideOffset` | integer | Finite integer number. | Falls back to endpointLabelSideOffset. | Side offset applied perpendicular to the link direction during source label auto placement. |
| `targetDistanceFromNode` | integer | Finite integer number. | No TopoViewer default; authored only. | Move rendered target endpoint inward from node boundary. |
| `targetLabelAutoPosition` | boolean | `true`, `false` | Falls back to endpointLabelAutoPosition, then true. | Whether the target endpoint label is automatically positioned near its endpoint. |
| `targetLabelBackgroundColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Target endpoint label background. |
| `targetLabelBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Target endpoint label border color. |
| `targetLabelBorderWidth` | integer | Finite integer number. | No TopoViewer default; authored only. | Target endpoint label border width. |
| `targetLabelColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Target endpoint label color. |
| `targetLabelDistance` | integer | Finite integer number. | Falls back to endpointLabelDistance. | Preferred distance between the target endpoint and its label. |
| `targetLabelFontSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Target endpoint label font size. |
| `targetLabelFontStyle` | text | String value. | No TopoViewer default; authored only. | Target endpoint label font style. |
| `targetLabelFontWeight` | text | String value. | No TopoViewer default; authored only. | Target endpoint label font weight. |
| `targetLabelMaxDistance` | integer | Finite integer number. | Falls back to endpointLabelMaxDistance. | Maximum distance the target endpoint label may move from its endpoint during auto placement. |
| `targetLabelOpacity` | number | Finite number. | Defaults to `1`. | Target endpoint label opacity. |
| `targetLabelOverlayLayer` | text | String value. | Falls back to endpointLabelOverlayLayer. | Toggle ID that controls the target endpoint label. |
| `targetLabelSideOffset` | integer | Finite integer number. | Falls back to endpointLabelSideOffset. | Side offset applied perpendicular to the link direction during target label auto placement. |
| `textBackgroundColor` | color | Any CSS color or supported theme variable. | Falls back to --topoviewer-edge-label-bg. | Shared edge label background. |
| `textBackgroundOpacity` | number | Finite number. | No TopoViewer default; authored only. | Shared edge label background opacity. |
| `anchor` | enum | `floating`, `fixed` | Defaults to `floating`. | Endpoint anchoring model. |
| `arrowColor` | color | Any CSS color or supported theme variable. | Falls back to the edge line color. | Shared arrow color fallback. |
| `controlPointDistance` | number | Finite number. | No TopoViewer default; authored only. | Manual Bezier control-point distance. |
| `controlPointStepSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Distance between same-endpoint Bezier control points. |
| `controlPointWeight` | number | Finite number. | Defaults to `0.5`. | Manual Bezier control-point weight. |
| `curveStyle` | enum | `straight`, `bezier`, `unbundledBezier`, `simpleBezier`, `segments`, `roundSegments`, `taxi`, `roundTaxi`, `smoothTaxi`, `smoothstep`, `haystack` | Defaults to `bezier`. | Edge route shape. |
| `display` | enum | `element`, `none` | Defaults to `element`. | Set none to hide the edge. |
| `edgeDistances` | enum | `intersection`, `nodePosition`, `endpoints` | No TopoViewer default; authored only. | Cytoscape-compatible edge distance hint. |
| `edgeLabelColor` | color | Any CSS color or supported theme variable. | Falls back to the rendered edge line color. | Center edge label color. |
| `interactive` | boolean | `true`, `false` | Defaults to `true`. | Whether edge click handling and focus are enabled. |
| `label` | text | String value. | No TopoViewer default; authored only. | Fallback center edge label. |
| `labelCollisionPolicy` | enum | `none`, `avoid`, `fade`, `hide` | Defaults to `avoid`. | How center labels, endpoint labels, and direction labels behave when automatic placement cannot avoid overlap. |
| `labelColor` | color | Any CSS color or supported theme variable. | Center labels prefer edgeLabelColor; source/target labels may still use labelColor as a shared fallback. | Shared legacy label color fallback. |
| `labelFontSize` | integer | Finite integer number. | Falls back to edge label CSS font size. Fallback: `10`. | Center edge label font size. |
| `labelFontStyle` | text | String value. | No TopoViewer default; authored only. | Center edge label font style. |
| `labelFontWeight` | text | String value. | Falls back to edge label CSS font weight. Fallback: `650`. | Center edge label font weight. |
| `labelXOffset` | integer | Finite integer number. | Directional links auto-offset the center label away from directional labels. Normal links use the midpoint. Explicit 0 pins the label to the midpoint. | Pixel X offset for the center edge label relative to the computed midpoint. |
| `labelYOffset` | integer | Finite integer number. | Directional links auto-offset the center label away from directional labels. Normal links use the midpoint. Explicit 0 pins the label to the midpoint. | Pixel Y offset for the center edge label relative to the computed midpoint. |
| `labelZIndex` | integer | Finite integer number. | No TopoViewer default; authored only. | Draw order for the center edge label. |
| `lineCap` | enum | `butt`, `round`, `square` | No TopoViewer default; authored only. | SVG stroke line cap. |
| `lineColor` | color | Any CSS color or supported theme variable. | Defaults to `#6ea8fe`. | Edge stroke color. |
| `lineDashOffset` | number | Finite number. | No TopoViewer default; authored only. | SVG dash phase offset. |
| `lineDashPattern` | numberList | Space-separated string, comma-separated string, number, or number array. | No TopoViewer default; authored only. | Explicit SVG line dash pattern. |
| `lineOpacity` | number | Finite number. | No TopoViewer default; authored only. | Line opacity without changing label opacity. |
| `lineStyle` | enum | `solid`, `dashed`, `dotted` | Defaults to `solid`. | Convenience line dash style. |
| `lineWidth` | integer | Finite integer number. | Defaults to `1`. | Edge stroke width. |
| `opacity` | number | Finite number. | No TopoViewer default; authored only. | Edge opacity. |
| `segmentDistances` | numberList | Space-separated string, comma-separated string, number, or number array. | No TopoViewer default; authored only. | Explicit bend distances for segment routing. |
| `segmentWeights` | numberList | Space-separated string, comma-separated string, number, or number array. | No TopoViewer default; authored only. | Explicit bend weights for segment routing. |
| `sourceArrowBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Source arrow marker outline color. |
| `sourceArrowBorderWidth` | number | Finite number. | Defaults to `0`. | Source arrow marker outline width. |
| `sourceArrowColor` | color | Any CSS color or supported theme variable. | Falls back to arrowColor, then lineColor. | Source arrow marker color. |
| `sourceArrowOffset` | number | Finite number. | Defaults to `0`. | Pixel offset for the source arrowhead. `0` keeps the arrow tip exactly on the rendered stroke endpoint; positive values inset it from that endpoint. |
| `sourceArrowShape` | enum | `none`, `triangle`, `vee`, `tee`, `circle`, `square`, `diamond` | Defaults to `none`. | Source arrow marker shape. |
| `sourceArrowSize` | integer | Finite integer number. | Falls back to the rendered lineWidth. | Source arrow marker size. |
| `sourceLabel` | text | String value. | No TopoViewer default; authored only. | Label rendered near the source endpoint. |
| `sourceLabelXOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel X offset applied to the source endpoint label. |
| `sourceLabelYOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel Y offset applied to the source endpoint label. |
| `sourceLabelZIndex` | integer | Finite integer number. | Falls back to labelZIndex when omitted. | Source endpoint label draw order. |
| `targetArrowBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Target arrow marker outline color. |
| `targetArrowBorderWidth` | number | Finite number. | Defaults to `0`. | Target arrow marker outline width. |
| `targetArrowColor` | color | Any CSS color or supported theme variable. | Falls back to arrowColor, then lineColor. | Target arrow marker color. |
| `targetArrowOffset` | number | Finite number. | Defaults to `0`. | Pixel offset for the target arrowhead. `0` keeps the arrow tip exactly on the rendered stroke endpoint; positive values inset it from that endpoint. |
| `targetArrowShape` | enum | `none`, `triangle`, `vee`, `tee`, `circle`, `square`, `diamond` | Defaults to `none`. | Target arrow marker shape. |
| `targetArrowSize` | integer | Finite integer number. | Falls back to the rendered lineWidth. | Target arrow marker size. |
| `targetLabel` | text | String value. | No TopoViewer default; authored only. | Label rendered near the target endpoint. |
| `targetLabelXOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel X offset applied to the target endpoint label. |
| `targetLabelYOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel Y offset applied to the target endpoint label. |
| `targetLabelZIndex` | integer | Finite integer number. | Falls back to labelZIndex when omitted. | Target endpoint label draw order. |
| `taxiDirection` | enum | `auto`, `vertical`, `downward`, `upward`, `horizontal`, `rightward`, `leftward` | Defaults to `auto`. | Primary taxi routing direction. |
| `taxiTurn` | text | String value. | No TopoViewer default; authored only. | Taxi turn placement. |
| `taxiTurnMinDistance` | integer | Finite integer number. | No TopoViewer default; authored only. | Minimum edge length before custom taxi routing applies. |
| `zIndex` | integer | Finite integer number. | Defaults to `6`. | Draw order for the edge line. |

## Link Direction Style Keys

`linkDirection` is the virtual selector subject for per-direction strokes declared under `graph.links[].directions`. It reuses the compatible link/path edge style surface.

| Key | Data Type | Values | Default | Use |
|---|---|---|---|---|
| `directionalStrokes` | boolean | `true`, `false` | Defaults to `false`. | Render declared link directions as opposing strokes on one physical link corridor. |
| `directionCenterGap` | integer | Finite integer number. | Defaults to `48`. | Gap between opposing directional arrowheads near the center of the link. |
| `directionLabelOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel offset applied to directional labels. |
| `directionLabelPlacement` | enum | `center`, `source`, `target`, `outside` | Defaults to `center`. | Directional label placement on the link corridor. |
| `directionLabelRotation` | enum | `none`, `auto` | Defaults to `none`. | Directional label rotation behavior. `auto` aligns labels to the rendered directional segment while keeping text upright. |
| `directionOverlayLayer` | text | String value. | No TopoViewer default; authored only. | Toggle ID that controls directional strokes and labels without hiding the base link object. |
| `directionStartGap` | integer | Finite integer number. | Defaults to `14`. | Inset between node boundary and each visible directional stroke. |
| `endpointLabelAutoPosition` | boolean | `true`, `false` | Defaults to `true`. | Shared default for source and target endpoint label auto placement. |
| `endpointLabelDistance` | integer | Finite integer number. | Defaults to lineWidth plus 14, with minimum 18. | Shared preferred endpoint label distance. |
| `endpointLabelMaxDistance` | integer | Finite integer number. | Defaults to endpointLabelDistance plus room for collision resolution. | Shared maximum endpoint label distance during auto placement. |
| `endpointLabelOverlayLayer` | text | String value. | No TopoViewer default; authored only. | Toggle ID that controls both source and target endpoint labels without hiding the edge object. |
| `endpointLabelSideOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Shared perpendicular offset used during endpoint label auto placement. This is applied before the final sourceLabelXOffset/YOffset and targetLabelXOffset/YOffset nudges. |
| `interactionWidth` | integer | Finite integer number. | Defaults to max(12, lineWidth + 10). | Pointer hit area. |
| `labelBorderColor` | color | Any CSS color or supported theme variable. | Falls back to --topoviewer-edge-label-border. | Shared edge label border color. |
| `labelBorderWidth` | integer | Finite integer number. | Falls back to edge label CSS border width. Fallback: `1`. | Shared edge label border width. |
| `labelInteractive` | boolean | `true`, `false` | Defaults to `true`. | Whether edge labels receive pointer events. |
| `lineFill` | enum | `solid`, `linearGradient` | Defaults to `solid`. | Line fill model. |
| `lineGradientStopColors` | text | String value. | No TopoViewer default; authored only. | Gradient stop colors when lineFill is linearGradient. |
| `lineGradientStopPositions` | text | String value. | No TopoViewer default; authored only. | Gradient stop positions when lineFill is linearGradient. |
| `lineOutlineColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Outline color behind the edge line. |
| `lineOutlineWidth` | integer | Finite integer number. | No TopoViewer default; authored only. | Outline width behind the edge line. |
| `sourceDistanceFromNode` | integer | Finite integer number. | No TopoViewer default; authored only. | Move rendered source endpoint inward from node boundary. |
| `sourceLabelAutoPosition` | boolean | `true`, `false` | Falls back to endpointLabelAutoPosition, then true. | Whether the source endpoint label is automatically positioned near its endpoint. |
| `sourceLabelBackgroundColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Source endpoint label background. |
| `sourceLabelBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Source endpoint label border color. |
| `sourceLabelBorderWidth` | integer | Finite integer number. | No TopoViewer default; authored only. | Source endpoint label border width. |
| `sourceLabelColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Source endpoint label color. |
| `sourceLabelDistance` | integer | Finite integer number. | Falls back to endpointLabelDistance. | Preferred distance between the source endpoint and its label. |
| `sourceLabelFontSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Source endpoint label font size. |
| `sourceLabelFontStyle` | text | String value. | No TopoViewer default; authored only. | Source endpoint label font style. |
| `sourceLabelFontWeight` | text | String value. | No TopoViewer default; authored only. | Source endpoint label font weight. |
| `sourceLabelMaxDistance` | integer | Finite integer number. | Falls back to endpointLabelMaxDistance. | Maximum distance the source endpoint label may move from its endpoint during auto placement. |
| `sourceLabelOpacity` | number | Finite number. | Defaults to `1`. | Source endpoint label opacity. |
| `sourceLabelOverlayLayer` | text | String value. | Falls back to endpointLabelOverlayLayer. | Toggle ID that controls the source endpoint label. |
| `sourceLabelSideOffset` | integer | Finite integer number. | Falls back to endpointLabelSideOffset. | Side offset applied perpendicular to the link direction during source label auto placement. |
| `targetDistanceFromNode` | integer | Finite integer number. | No TopoViewer default; authored only. | Move rendered target endpoint inward from node boundary. |
| `targetLabelAutoPosition` | boolean | `true`, `false` | Falls back to endpointLabelAutoPosition, then true. | Whether the target endpoint label is automatically positioned near its endpoint. |
| `targetLabelBackgroundColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Target endpoint label background. |
| `targetLabelBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Target endpoint label border color. |
| `targetLabelBorderWidth` | integer | Finite integer number. | No TopoViewer default; authored only. | Target endpoint label border width. |
| `targetLabelColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Target endpoint label color. |
| `targetLabelDistance` | integer | Finite integer number. | Falls back to endpointLabelDistance. | Preferred distance between the target endpoint and its label. |
| `targetLabelFontSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Target endpoint label font size. |
| `targetLabelFontStyle` | text | String value. | No TopoViewer default; authored only. | Target endpoint label font style. |
| `targetLabelFontWeight` | text | String value. | No TopoViewer default; authored only. | Target endpoint label font weight. |
| `targetLabelMaxDistance` | integer | Finite integer number. | Falls back to endpointLabelMaxDistance. | Maximum distance the target endpoint label may move from its endpoint during auto placement. |
| `targetLabelOpacity` | number | Finite number. | Defaults to `1`. | Target endpoint label opacity. |
| `targetLabelOverlayLayer` | text | String value. | Falls back to endpointLabelOverlayLayer. | Toggle ID that controls the target endpoint label. |
| `targetLabelSideOffset` | integer | Finite integer number. | Falls back to endpointLabelSideOffset. | Side offset applied perpendicular to the link direction during target label auto placement. |
| `textBackgroundColor` | color | Any CSS color or supported theme variable. | Falls back to --topoviewer-edge-label-bg. | Shared edge label background. |
| `textBackgroundOpacity` | number | Finite number. | No TopoViewer default; authored only. | Shared edge label background opacity. |
| `anchor` | enum | `floating`, `fixed` | Defaults to `floating`. | Endpoint anchoring model. |
| `arrowColor` | color | Any CSS color or supported theme variable. | Falls back to the edge line color. | Shared arrow color fallback. |
| `controlPointDistance` | number | Finite number. | No TopoViewer default; authored only. | Manual Bezier control-point distance. |
| `controlPointStepSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Distance between same-endpoint Bezier control points. |
| `controlPointWeight` | number | Finite number. | Defaults to `0.5`. | Manual Bezier control-point weight. |
| `curveStyle` | enum | `straight`, `bezier`, `unbundledBezier`, `simpleBezier`, `segments`, `roundSegments`, `taxi`, `roundTaxi`, `smoothTaxi`, `smoothstep`, `haystack` | Defaults to `bezier`. | Edge route shape. |
| `display` | enum | `element`, `none` | Defaults to `element`. | Set none to hide the edge. |
| `edgeDistances` | enum | `intersection`, `nodePosition`, `endpoints` | No TopoViewer default; authored only. | Cytoscape-compatible edge distance hint. |
| `edgeLabelColor` | color | Any CSS color or supported theme variable. | Falls back to the rendered edge line color. | Center edge label color. |
| `interactive` | boolean | `true`, `false` | Defaults to `true`. | Whether edge click handling and focus are enabled. |
| `label` | text | String value. | No TopoViewer default; authored only. | Fallback center edge label. |
| `labelCollisionPolicy` | enum | `none`, `avoid`, `fade`, `hide` | Defaults to `avoid`. | How center labels, endpoint labels, and direction labels behave when automatic placement cannot avoid overlap. |
| `labelColor` | color | Any CSS color or supported theme variable. | Center labels prefer edgeLabelColor; source/target labels may still use labelColor as a shared fallback. | Shared legacy label color fallback. |
| `labelFontSize` | integer | Finite integer number. | Falls back to edge label CSS font size. Fallback: `10`. | Center edge label font size. |
| `labelFontStyle` | text | String value. | No TopoViewer default; authored only. | Center edge label font style. |
| `labelFontWeight` | text | String value. | Falls back to edge label CSS font weight. Fallback: `650`. | Center edge label font weight. |
| `labelXOffset` | integer | Finite integer number. | Directional links auto-offset the center label away from directional labels. Normal links use the midpoint. Explicit 0 pins the label to the midpoint. | Pixel X offset for the center edge label relative to the computed midpoint. |
| `labelYOffset` | integer | Finite integer number. | Directional links auto-offset the center label away from directional labels. Normal links use the midpoint. Explicit 0 pins the label to the midpoint. | Pixel Y offset for the center edge label relative to the computed midpoint. |
| `labelZIndex` | integer | Finite integer number. | No TopoViewer default; authored only. | Draw order for the center edge label. |
| `lineCap` | enum | `butt`, `round`, `square` | No TopoViewer default; authored only. | SVG stroke line cap. |
| `lineColor` | color | Any CSS color or supported theme variable. | Defaults to `#6ea8fe`. | Edge stroke color. |
| `lineDashOffset` | number | Finite number. | No TopoViewer default; authored only. | SVG dash phase offset. |
| `lineDashPattern` | numberList | Space-separated string, comma-separated string, number, or number array. | No TopoViewer default; authored only. | Explicit SVG line dash pattern. |
| `lineOpacity` | number | Finite number. | No TopoViewer default; authored only. | Line opacity without changing label opacity. |
| `lineStyle` | enum | `solid`, `dashed`, `dotted` | Defaults to `solid`. | Convenience line dash style. |
| `lineWidth` | integer | Finite integer number. | Defaults to `1`. | Edge stroke width. |
| `opacity` | number | Finite number. | No TopoViewer default; authored only. | Edge opacity. |
| `segmentDistances` | numberList | Space-separated string, comma-separated string, number, or number array. | No TopoViewer default; authored only. | Explicit bend distances for segment routing. |
| `segmentWeights` | numberList | Space-separated string, comma-separated string, number, or number array. | No TopoViewer default; authored only. | Explicit bend weights for segment routing. |
| `sourceArrowBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Source arrow marker outline color. |
| `sourceArrowBorderWidth` | number | Finite number. | Defaults to `0`. | Source arrow marker outline width. |
| `sourceArrowColor` | color | Any CSS color or supported theme variable. | Falls back to arrowColor, then lineColor. | Source arrow marker color. |
| `sourceArrowOffset` | number | Finite number. | Defaults to `0`. | Pixel offset for the source arrowhead. `0` keeps the arrow tip exactly on the rendered stroke endpoint; positive values inset it from that endpoint. |
| `sourceArrowShape` | enum | `none`, `triangle`, `vee`, `tee`, `circle`, `square`, `diamond` | Defaults to `none`. | Source arrow marker shape. |
| `sourceArrowSize` | integer | Finite integer number. | Falls back to the rendered lineWidth. | Source arrow marker size. |
| `sourceLabel` | text | String value. | No TopoViewer default; authored only. | Label rendered near the source endpoint. |
| `sourceLabelXOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel X offset applied to the source endpoint label. |
| `sourceLabelYOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel Y offset applied to the source endpoint label. |
| `sourceLabelZIndex` | integer | Finite integer number. | Falls back to labelZIndex when omitted. | Source endpoint label draw order. |
| `targetArrowBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Target arrow marker outline color. |
| `targetArrowBorderWidth` | number | Finite number. | Defaults to `0`. | Target arrow marker outline width. |
| `targetArrowColor` | color | Any CSS color or supported theme variable. | Falls back to arrowColor, then lineColor. | Target arrow marker color. |
| `targetArrowOffset` | number | Finite number. | Defaults to `0`. | Pixel offset for the target arrowhead. `0` keeps the arrow tip exactly on the rendered stroke endpoint; positive values inset it from that endpoint. |
| `targetArrowShape` | enum | `none`, `triangle`, `vee`, `tee`, `circle`, `square`, `diamond` | Defaults to `none`. | Target arrow marker shape. |
| `targetArrowSize` | integer | Finite integer number. | Falls back to the rendered lineWidth. | Target arrow marker size. |
| `targetLabel` | text | String value. | No TopoViewer default; authored only. | Label rendered near the target endpoint. |
| `targetLabelXOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel X offset applied to the target endpoint label. |
| `targetLabelYOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel Y offset applied to the target endpoint label. |
| `targetLabelZIndex` | integer | Finite integer number. | Falls back to labelZIndex when omitted. | Target endpoint label draw order. |
| `taxiDirection` | enum | `auto`, `vertical`, `downward`, `upward`, `horizontal`, `rightward`, `leftward` | Defaults to `auto`. | Primary taxi routing direction. |
| `taxiTurn` | text | String value. | No TopoViewer default; authored only. | Taxi turn placement. |
| `taxiTurnMinDistance` | integer | Finite integer number. | No TopoViewer default; authored only. | Minimum edge length before custom taxi routing applies. |
| `zIndex` | integer | Finite integer number. | Defaults to `6`. | Draw order for the edge line. |

## Path Style Keys

| Key | Data Type | Values | Default | Use |
|---|---|---|---|---|
| `anchor` | enum | `floating`, `fixed` | Defaults to `floating`. | Endpoint anchoring model. |
| `arrowColor` | color | Any CSS color or supported theme variable. | Falls back to the edge line color. | Shared arrow color fallback. |
| `controlPointDistance` | number | Finite number. | No TopoViewer default; authored only. | Manual Bezier control-point distance. |
| `controlPointStepSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Distance between same-endpoint Bezier control points. |
| `controlPointWeight` | number | Finite number. | Defaults to `0.5`. | Manual Bezier control-point weight. |
| `curveStyle` | enum | `straight`, `bezier`, `unbundledBezier`, `simpleBezier`, `segments`, `roundSegments`, `taxi`, `roundTaxi`, `smoothTaxi`, `smoothstep`, `haystack` | Defaults to `bezier`. | Edge route shape. |
| `display` | enum | `element`, `none` | Defaults to `element`. | Set none to hide the edge. |
| `edgeDistances` | enum | `intersection`, `nodePosition`, `endpoints` | No TopoViewer default; authored only. | Cytoscape-compatible edge distance hint. |
| `edgeLabelColor` | color | Any CSS color or supported theme variable. | Falls back to the rendered edge line color. | Center edge label color. |
| `interactive` | boolean | `true`, `false` | Defaults to `true`. | Whether edge click handling and focus are enabled. |
| `label` | text | String value. | No TopoViewer default; authored only. | Fallback center edge label. |
| `labelCollisionPolicy` | enum | `none`, `avoid`, `fade`, `hide` | Defaults to `avoid`. | How center labels, endpoint labels, and direction labels behave when automatic placement cannot avoid overlap. |
| `labelColor` | color | Any CSS color or supported theme variable. | Center labels prefer edgeLabelColor; source/target labels may still use labelColor as a shared fallback. | Shared legacy label color fallback. |
| `labelFontSize` | integer | Finite integer number. | Falls back to edge label CSS font size. Fallback: `10`. | Center edge label font size. |
| `labelFontStyle` | text | String value. | No TopoViewer default; authored only. | Center edge label font style. |
| `labelFontWeight` | text | String value. | Falls back to edge label CSS font weight. Fallback: `650`. | Center edge label font weight. |
| `labelXOffset` | integer | Finite integer number. | Directional links auto-offset the center label away from directional labels. Normal links use the midpoint. Explicit 0 pins the label to the midpoint. | Pixel X offset for the center edge label relative to the computed midpoint. |
| `labelYOffset` | integer | Finite integer number. | Directional links auto-offset the center label away from directional labels. Normal links use the midpoint. Explicit 0 pins the label to the midpoint. | Pixel Y offset for the center edge label relative to the computed midpoint. |
| `labelZIndex` | integer | Finite integer number. | No TopoViewer default; authored only. | Draw order for the center edge label. |
| `lineCap` | enum | `butt`, `round`, `square` | No TopoViewer default; authored only. | SVG stroke line cap. |
| `lineColor` | color | Any CSS color or supported theme variable. | Defaults to `#6ea8fe`. | Edge stroke color. |
| `lineDashOffset` | number | Finite number. | No TopoViewer default; authored only. | SVG dash phase offset. |
| `lineDashPattern` | numberList | Space-separated string, comma-separated string, number, or number array. | No TopoViewer default; authored only. | Explicit SVG line dash pattern. |
| `lineOpacity` | number | Finite number. | No TopoViewer default; authored only. | Line opacity without changing label opacity. |
| `lineStyle` | enum | `solid`, `dashed`, `dotted` | Defaults to `solid`. | Convenience line dash style. |
| `lineWidth` | integer | Finite integer number. | Defaults to `1`. | Edge stroke width. |
| `opacity` | number | Finite number. | No TopoViewer default; authored only. | Edge opacity. |
| `segmentDistances` | numberList | Space-separated string, comma-separated string, number, or number array. | No TopoViewer default; authored only. | Explicit bend distances for segment routing. |
| `segmentWeights` | numberList | Space-separated string, comma-separated string, number, or number array. | No TopoViewer default; authored only. | Explicit bend weights for segment routing. |
| `sourceArrowBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Source arrow marker outline color. |
| `sourceArrowBorderWidth` | number | Finite number. | Defaults to `0`. | Source arrow marker outline width. |
| `sourceArrowColor` | color | Any CSS color or supported theme variable. | Falls back to arrowColor, then lineColor. | Source arrow marker color. |
| `sourceArrowOffset` | number | Finite number. | Defaults to `0`. | Pixel offset for the source arrowhead. `0` keeps the arrow tip exactly on the rendered stroke endpoint; positive values inset it from that endpoint. |
| `sourceArrowShape` | enum | `none`, `triangle`, `vee`, `tee`, `circle`, `square`, `diamond` | Defaults to `none`. | Source arrow marker shape. |
| `sourceArrowSize` | integer | Finite integer number. | Falls back to the rendered lineWidth. | Source arrow marker size. |
| `sourceLabel` | text | String value. | No TopoViewer default; authored only. | Label rendered near the source endpoint. |
| `sourceLabelXOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel X offset applied to the source endpoint label. |
| `sourceLabelYOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel Y offset applied to the source endpoint label. |
| `sourceLabelZIndex` | integer | Finite integer number. | Falls back to labelZIndex when omitted. | Source endpoint label draw order. |
| `targetArrowBorderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Target arrow marker outline color. |
| `targetArrowBorderWidth` | number | Finite number. | Defaults to `0`. | Target arrow marker outline width. |
| `targetArrowColor` | color | Any CSS color or supported theme variable. | Falls back to arrowColor, then lineColor. | Target arrow marker color. |
| `targetArrowOffset` | number | Finite number. | Defaults to `0`. | Pixel offset for the target arrowhead. `0` keeps the arrow tip exactly on the rendered stroke endpoint; positive values inset it from that endpoint. |
| `targetArrowShape` | enum | `none`, `triangle`, `vee`, `tee`, `circle`, `square`, `diamond` | Defaults to `none`. | Target arrow marker shape. |
| `targetArrowSize` | integer | Finite integer number. | Falls back to the rendered lineWidth. | Target arrow marker size. |
| `targetLabel` | text | String value. | No TopoViewer default; authored only. | Label rendered near the target endpoint. |
| `targetLabelXOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel X offset applied to the target endpoint label. |
| `targetLabelYOffset` | integer | Finite integer number. | No TopoViewer default; authored only. | Pixel Y offset applied to the target endpoint label. |
| `targetLabelZIndex` | integer | Finite integer number. | Falls back to labelZIndex when omitted. | Target endpoint label draw order. |
| `taxiDirection` | enum | `auto`, `vertical`, `downward`, `upward`, `horizontal`, `rightward`, `leftward` | Defaults to `auto`. | Primary taxi routing direction. |
| `taxiTurn` | text | String value. | No TopoViewer default; authored only. | Taxi turn placement. |
| `taxiTurnMinDistance` | integer | Finite integer number. | No TopoViewer default; authored only. | Minimum edge length before custom taxi routing applies. |
| `zIndex` | integer | Finite integer number. | Defaults to `6`. | Draw order for the edge line. |
| `animated` | boolean | `true`, `false` | Defaults to `false`. | Enable React Flow edge animation. |
| `laneGap` | integer | Finite integer number. | Defaults to `5`. | Child path lane spacing. |
| `laneWidth` | integer | Finite integer number. | Falls back to lineWidth, then 3. | Child path lane width. |
| `pipe` | boolean | `true`, `false` | Automatically enabled when the object has visible child paths. | Render a parent path as a pipe corridor. |
| `pipeBorderColor` | color | Any CSS color or supported theme variable. | Falls back to lineColor. | Outer pipe border color. |
| `pipeBorderWidth` | integer | Finite integer number. | Defaults to `2`. | Outer pipe border width. |
| `pipeFill` | color | Any CSS color or supported theme variable. | Falls back to lineColor. | Fill color for the parent pipe. |
| `pipeOpacity` | number | Finite number. | Defaults to `0.18`. | Parent pipe opacity. |
| `pipeWidth` | integer | Finite integer number. | Falls back to lineWidth plus 14, with minimum 18. | Width of the parent pipe fill. |

## Region Style Keys

| Key | Data Type | Values | Default | Use |
|---|---|---|---|---|
| `backgroundColor` | color | Any CSS color or supported theme variable. | Defaults to `rgba(76, 201, 240, 0.12)`. | Region fill. |
| `borderColor` | color | Any CSS color or supported theme variable. | Defaults to `rgba(76, 201, 240, 0.62)`. | Region border color. |
| `borderWidth` | integer | Finite integer number. | Defaults to `1`. | Region border width. |
| `draggable` | boolean | `true`, `false` | Defaults to `false`. | Whether the region hull can be dragged. |
| `labelBackgroundColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Region label background. |
| `labelCollisionPolicy` | enum | `none`, `avoid`, `fade`, `hide` | Defaults to `avoid`. | How the region label behaves when automatic placement cannot avoid overlap. |
| `labelColor` | color | Any CSS color or supported theme variable. | Falls back to region label CSS/theme styling. | Region label color. |
| `labelFontSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Region label font size. |
| `labelFontWeight` | text | String value. | No TopoViewer default; authored only. | Region label font weight. |
| `labelMargin` | integer | Finite integer number. | Defaults to `12`. | Region label margin from the selected region edge. |
| `labelPosition` | enum | `topLeft`, `topCenter`, `topRight`, `rightTop`, `rightCenter`, `rightBottom`, `bottomRight`, `bottomCenter`, `bottomLeft`, `leftTop`, `leftCenter`, `leftBottom` | Defaults to `topLeft`. | Region label anchor. |
| `labelZIndex` | integer | Finite integer number. | No TopoViewer default; authored only. | Independent draw order for the region label. |
| `opacity` | number | Finite number. | No TopoViewer default; authored only. | Region opacity. |
| `selectable` | boolean | `true`, `false` | Defaults to `false`. | Whether the region hull can be selected. |
| `shape` | enum | `roundRectangle`, `rectangle`, `ellipse` | Defaults to `roundRectangle`. | Region hull shape. |
| `zIndex` | integer | Finite integer number. | Defaults to `-20`. | Draw order for the region hull. |

## Shape Style Keys

| Key | Data Type | Values | Default | Use |
|---|---|---|---|---|
| `backgroundColor` | color | Any CSS color or supported theme variable. | Falls back to fill when authored. | Alias-style fill input for shape fill. |
| `borderColor` | color | Any CSS color or supported theme variable. | Falls back to stroke when authored. | Alias-style stroke input for shape stroke. |
| `borderWidth` | integer | Finite integer number. | Falls back to strokeWidth when authored. | Alias-style stroke width input for shape stroke width. |
| `boxShadow` | text | String value. | No TopoViewer default; authored only. | Presentation depth for the shape container. |
| `display` | enum | `element`, `none` | Defaults to `element`. | Set none to hide the shape. |
| `draggable` | boolean | `true`, `false` | Defaults to `true`. | Whether the shape can be dragged unless locked. |
| `fill` | color | Any CSS color or supported theme variable. | Defaults to `rgba(38, 54, 72, 0.82)`. | Shape fill. |
| `height` | integer | Finite integer number. | Defaults to `72`. | Default shape height when the object has no size. |
| `labelZIndex` | integer | Finite integer number. | No TopoViewer default; authored only. | Independent draw order for a shape label where rendered. |
| `opacity` | number | Finite number. | No TopoViewer default; authored only. | Shape opacity. |
| `rotation` | integer | Finite integer number. | Falls back to object rotation, then 0. Fallback: `0`. | Geometry rotation in degrees. |
| `selectable` | boolean | `true`, `false` | Defaults to `false`. | Whether the shape can be selected unless locked. |
| `shape` | enum | `circle`, `triangle`, `square`, `rectangle`, `pentagon`, `hexagon`, `octagon`, `ellipse`, `semicircle`, `trapezoid`, `parallelogram`, `rhombus`, `kite`, `star`, `cube`, `cuboid`, `sphere`, `cone`, `cylinder`, `pyramid`, `prism` | Falls back to the object type, then rectangle. Fallback: `rectangle`. | Diagram shape geometry. |
| `stroke` | color | Any CSS color or supported theme variable. | Defaults to `rgba(148, 163, 184, 0.64)`. | Shape stroke. |
| `strokeWidth` | integer | Finite integer number. | Defaults to `2`. | Shape stroke width. |
| `width` | integer | Finite integer number. | Defaults to `180`. | Default shape width when the object has no size. |
| `zIndex` | integer | Finite integer number. | Defaults to `-10`. | Draw order for the shape. |

## Callout Style Keys

| Key | Data Type | Values | Default | Use |
|---|---|---|---|---|
| `backgroundColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Callout body fill. |
| `bodyColor` | color | Any CSS color or supported theme variable. | Falls back to shared text color. | Callout body text color. |
| `bodyFontSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Callout body font size. |
| `bodyFontWeight` | text | String value. | No TopoViewer default; authored only. | Callout body font weight. |
| `bodyLineHeight` | number | Finite number. | No TopoViewer default; authored only. | Callout body line height. |
| `borderColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Callout border color. |
| `borderRadius` | integer | Finite integer number. | No TopoViewer default; authored only. | Callout border radius. |
| `borderWidth` | integer | Finite integer number. | No TopoViewer default; authored only. | Callout border width. |
| `boxShadow` | text | String value. | No TopoViewer default; authored only. | Presentation depth for the callout box. |
| `color` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Shared callout text color. |
| `display` | enum | `element`, `none` | Defaults to `element`. | Set none to hide the callout. |
| `draggable` | boolean | `true`, `false` | Defaults to `true`. | Whether the callout can be dragged unless locked. |
| `height` | integer | Finite integer number. | Defaults to `120`. | Default callout height when the object has no size. |
| `labelZIndex` | integer | Finite integer number. | No TopoViewer default; authored only. | Independent draw order for a callout label where rendered. |
| `opacity` | number | Finite number. | No TopoViewer default; authored only. | Callout opacity. |
| `selectable` | boolean | `true`, `false` | Defaults to `false`. | Whether the callout can be selected unless locked. |
| `textAlign` | enum | `left`, `center`, `right` | Defaults to `left`. | Callout markdown text alignment. |
| `titleBackgroundColor` | color | Any CSS color or supported theme variable. | No TopoViewer default; authored only. | Callout title background. |
| `titleColor` | color | Any CSS color or supported theme variable. | Falls back to shared text color. | Callout title color. |
| `titleFontSize` | integer | Finite integer number. | No TopoViewer default; authored only. | Callout title font size. |
| `titleFontWeight` | text | String value. | No TopoViewer default; authored only. | Callout title font weight. |
| `width` | integer | Finite integer number. | Defaults to `320`. | Default callout width when the object has no size. |
| `zIndex` | integer | Finite integer number. | Defaults to `30`. | Draw order for the callout. |

## Drift Guard

Run this before publishing docs:

```bash
npm run check:object-reference
```

The check fails if this page is not regenerated after style-registry changes. `npm run sync:docs` regenerates it.
