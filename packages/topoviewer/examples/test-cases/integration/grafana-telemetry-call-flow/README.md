Internal fixture for the Grafana telemetry call-flow architecture page.

The public page uses this topology and stylesheet as a tested live viewport, but
the explanation is maintained as architecture documentation instead of a
generated example page.
