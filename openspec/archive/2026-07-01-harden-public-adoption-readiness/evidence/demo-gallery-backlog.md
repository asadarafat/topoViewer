# Demo Gallery Backlog

The curated Examples Gallery now includes one non-network infrastructure example
that is copyable today:

| Example | Evidence |
|---|---|
| Kubernetes service map | `packages/topoviewer/content/examples/integration/kubernetes-service-map/` and `packages/topoviewer/content/pages/examples/examples-gallery.md` |

Backlog for future visually compelling examples:

| Demo | Why it matters |
|---|---|
| AWS VPC topology | Cloud engineers can understand the value without network-provider context. |
| Kubernetes service map | Platform/SRE users see service, namespace, and data-dependency use cases. Initial version exists. |
| BGP/CLOS fabric | Network engineers can evaluate fabric layout, directional links, and labels. |
| Microservice dependency graph | Product/platform teams can model service ownership and shared dependencies. |
| Incident blast-radius view | Demonstrates attention, dimming, and runtime state as an operational workflow. |
| Grafana live overlay | Shows mounted bundles, mapper rules, and Prometheus-driven runtime styles. |

Gallery quality rule: curated examples should show `Live Viewport`, `Copy
Topology YAML`, `Copy Stylesheet YAML`, `What this proves`, and `Use this when`
before sending users into the generated reference catalog.

