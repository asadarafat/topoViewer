# MkDocs Python Install Path

This capability makes the MkDocs adapter installable from PyPI as
`mkdocs-topoviewer` while preserving the internal Python module name
`mkdocs_topoviewer` and the MkDocs plugin key `topoviewer`.
