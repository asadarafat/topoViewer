name: Docs

on:
  push:
    branches:
      - development
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build documentation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version: "1.25.12"
          cache-dependency-path: packages/grafana-topoviewer-panel/go.sum

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          npm ci
          python -m pip install --upgrade pip
          python -m pip install mkdocs-material

      - name: Install Chromium for Playwright
        run: npx playwright install --with-deps chromium

      - name: Report environment
        run: npm run ci:env

      - name: Build and smoke documentation targets
        run: npm run ci:docs

      - name: Build MkDocs wheel
        run: |
          npm run wheel:mkdocs
          python -m pip install .artifacts/mkdocs-dist/*.whl

      - name: Upload failure artifacts
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: docs-failure-artifacts
          retention-days: 3
          if-no-files-found: ignore
          path: |
            site
            .artifacts
            packages/topoviewer/test-results
            packages/topoviewer/playwright-report
            packages/vscode-topoviewer/test-results
            packages/vscode-topoviewer/playwright-report

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: site

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    outputs:
      page_url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5

  verify-studio:
    name: Verify deployed Studio
    needs: deploy
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Smoke deployed Studio authoring surface
        run: node scripts/smoke-deployed-studio.mjs "${{ needs.deploy.outputs.page_url }}"
