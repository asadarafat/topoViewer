{
	"image": "ghcr.io/srl-labs/containerlab/clab-devcontainer:0.59.0",
	"features": {
	},
	
	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	"remoteUser": "vscode",


	// Configure tool-specific properties.
	"customizations": {
		// Configure properties specific to VS Code.
		"vscode": {
			// Set *default* container specific settings.json values on container create.
			"settings": {
				"go.toolsManagement.checkForUpdates": "local",
				"go.useLanguageServer": true,
				"go.gopath": "/go"
			}
		}
	},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// to EdgeShark and TopoViewer
	"forwardPorts": [5001, 8080],

	// Use 'postCreateCommand' to run commands after the container is created.
    "postCreateCommand": "apt-get update && sudo apt-get install -y openssh-server docker-compose && sudo service ssh start && curl -sL https://github.com/siemens/edgeshark/raw/main/deployments/wget/docker-compose.yaml | DOCKER_DEFAULT_PLATFORM=linux/amd64 docker compose -f - up -d && sudo sed -i 's/^Port 2222/Port 22/' /etc/ssh/sshd_config && service ssh restart && echo 'vscode:vscode' | sudo chpasswd && echo 'KexAlgorithms diffie-hellman-group1-sha1,curve25519-sha256@libssh.org,ecdh-sha2-nistp256,ecdh-sha2-nistp384,ecdh-sha2-nistp521,diffie-hellman-group-exchange-sha256,diffie-hellman-group14-sha1' >> /etc/ssh/ssh_config && sudo bash -c '$(curl -sL https://raw.githubusercontent.com/asadarafat/topoViewer/refs/heads/development/docs/quickstart/quick-deploy.sh)' && export TOPOVIEWER_HOST_CLAB=127.0.0.1 && export TOPOVIEWER_CLAB_ADDRESS=172.20.20.1 && TOPOVIEWER_HOST_CLAB_USER=vscode && TOPOVIEWER_HOST_CLAB_PASS=vscode && TOPOVIEWER_SERVER_PORT=8080 && TOPOVIEWER_CLAB_TOPO_YAML=clab-demo-output.yaml"

}
