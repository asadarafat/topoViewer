{
	"image": "ghcr.io/srl-labs/containerlab/clab-devcontainer:0.59.0",
	"features": {},
	"remoteUser": "vscode",
	"customizations": {
	  "vscode": {
		"settings": {
		  "go.toolsManagement.checkForUpdates": "local",
		  "go.useLanguageServer": true,
		  "go.gopath": "/go"
		}
	  }
	},
	"forwardPorts": [5001, 8080],
	"postCreateCommand": "sudo bash -c 'apt-get update && apt-get install -y openssh-server docker-compose && service ssh start && curl -sL https://github.com/siemens/edgeshark/raw/main/deployments/wget/docker-compose.yaml | DOCKER_DEFAULT_PLATFORM=linux/amd64 docker compose -f - up -d && sed -i \"s/^Port 2222/Port 22/\" /etc/ssh/sshd_config && service ssh restart && echo \"vscode:vscode\" | chpasswd && echo \"KexAlgorithms diffie-hellman-group1-sha1,curve25519-sha256@libssh.org,ecdh-sha2-nistp256,ecdh-sha2-nistp384,ecdh-sha2-nistp521,diffie-hellman-group-exchange-sha256,diffie-hellman-group14-sha1\" >> /etc/ssh/ssh_config && export TOPOVIEWER_HOST_CLAB=127.0.0.1 && export TOPOVIEWER_CLAB_ADDRESS=172.20.20.1 && export TOPOVIEWER_HOST_CLAB_USER=vscode && export TOPOVIEWER_HOST_CLAB_PASS=vscode && export TOPOVIEWER_SERVER_PORT=8080 && export TOPOVIEWER_CLAB_TOPO_YAML=clab-demo-output.yaml && curl -o \"$TOPOVIEWER_CLAB_TOPO_YAML\" -L \"https://raw.githubusercontent.com/asadarafat/topoViewer/refs/heads/development/docs/quickstart/clab-demo.yaml\" && clab deploy -t \"$TOPOVIEWER_CLAB_TOPO_YAML\"'"
  }
  